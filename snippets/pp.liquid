{% case media.media_type % }
    {% when 'image' %}
        <img  src="{{ media | image_url: width:650,height:300 }}" alt="{{ media.preview_image.alt }}" width=1100 height=600 class="w-full h-full object-cover">
    {% else %}    
{% endcase %}