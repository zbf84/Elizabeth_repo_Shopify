{% case media.media_type % }
    {% when 'image' %}
        <img width="500px" height="400px" src="{{ media | image_url }}" alt="{{ media.preview_image.alt }}" class="w-full h-full object-cover">
{% endcase %}